import styled from "styled-components";

const BrandIcon = styled.i.attrs({
    className: "fab fa-cloudversify"
})`
    margin-right: .5rem;
    vertical-align: middle;
`;

export default BrandIcon;
